# BITGO ACCELERATE TRANSACTION

1. intall packages (npm i)
2. configure system
    * rename `.env-example` to `.env`
    * provide walletID and API TOKEN
3. execute code by running `node index.js --tx="xxx"` where xxx is a transaction to accelerate.